# FNAI V1 开发阶段重排（16 周 / 4 个月）

> 适用：FNAI / GEO 内容生产平台
> 团队：7 人（3 后端 + 2 前端 + 2 AI/算法）
> 制定者：Senior Developer
> 制定时间：2026-06-18
> 上游依据：[`v1-scope-cut-list.md`](./v1-scope-cut-list.md)

---

## 一、阶段全景图

```
W1-W2   ┃ 阶段 1：工程化基础（infra + 规范 + CI）
W3-W4   ┃ 阶段 2：认证与多租户
W5-W7   ┃ 阶段 3：知识库与 RAG
W8-W11  ┃ 阶段 4：文章生成核心
W12-W13 ┃ 阶段 5：发布与导出
W14-W15 ┃ 阶段 6：联调 / 压测 / 安全审计
W16     ┃ 阶段 7：灰度发布与内测
```

每阶段用统一的 6 字段描述：**目标 / 交付物 / DoD / 负责人 / 风险 / 退出条件**。

---

## 二、阶段 1：工程化基础（W1-W2）

### 目标

把所有团队**第一天就需要的脚手架**一次性铺好：仓库结构、配置规范、CI、监控、文档规范。

### 交付物

| # | 交付物 | 路径 |
|---|--------|------|
| 1 | Monorepo 骨架 | `backend/`、`frontend/`、`admin/`、`packages/shared/`、`infra/` |
| 2 | 后端最小可运行 FastAPI | `backend/app/main.py` + `/health` |
| 3 | 前端最小可运行 Vite+React | `frontend/src/main.tsx` + 首页 |
| 4 | Docker Compose 一键起开发环境 | `infra/docker-compose.yml` |
| 5 | CI 工作流 | `.github/workflows/ci.yml`（或 `gitee-ci.yml`） |
| 6 | Pre-commit Hooks | `.pre-commit-config.yaml` |
| 7 | 代码审查 Checklist | `docs/code-review-checklist.md` |
| 8 | 提交规范 | `docs/commit-convention.md` |
| 9 | ADR 模板 | `docs/decisions/0000-template.md` + `0001-v1-scope.md` |
| 10 | Sentry + Prometheus 接入 | `infra/`、后端中间件 |

### Definition of Done

- [ ] `git clone` 后 `docker compose up -d` 5 分钟内能起服务
- [ ] 后端 `curl http://localhost:8000/health` 返回 `{"status":"ok"}`
- [ ] 前端 `http://localhost:5173` 能看到首页
- [ ] CI 在 PR 触发后能在 5 分钟内跑完 lint + type-check + test
- [ ] Pre-commit 装好后，改一行代码 `git commit` 会自动跑格式检查
- [ ] 团队 7 人完成 1 小时规范宣讲，所有人能在 30 分钟内通过规范 PR

### 负责人

- **后端 A**（主程）：仓库结构、后端脚手架、CI、ADR
- **前端 A**：前端脚手架、Tailwind 配置
- **苏苏**：规范宣讲、质量官轮值表

### 风险

| 风险 | 缓解 |
|------|------|
| Docker 在 Windows 上性能差 | 文档提供 WSL2 指引；后端用 `--reload` 热重载 |
| 团队抗拒 Pre-commit | 只检查格式不阻塞提交；错误信息必须友好 |
| CI 跑太慢 | 缓存依赖；并行跑 lint/test/build |

### 退出条件

进入阶段 2 前，必须满足：
1. 7 人都成功跑起来本地环境
2. 至少 2 个示例 PR 通过了 CI 和代码审查
3. 代码审查 Checklist 已被使用 1 次

---

## 三、阶段 2：认证与多租户（W3-W4）

### 目标

建立用户体系，把"这是谁的请求"这件事钉死。

### 交付物

| # | 交付物 | 说明 |
|---|--------|------|
| 1 | `tenant`、`user`、`tenant_member` 模型 | 22 张表中的 3 张 |
| 2 | 注册/登录/刷新/登出 4 个 API | `/api/v1/auth/*` |
| 3 | JWT 双 Token 机制 | access 15min + refresh 7d |
| 4 | 租户上下文中间件 | 每个请求自动注入 `tenant_id` |
| 5 | PostgreSQL RLS 策略 | `app.tenant_id` session 变量 + row filter |
| 6 | Alembic 初始迁移 | `0001_init_tenants_users.py` |
| 7 | 登录/注册前端页面 | 含表单校验、错误提示 |
| 8 | Auth 集成测试 | pytest + TestClient |

### DoD

- [ ] 注册→登录→访问受保护资源 完整 E2E 测试通过
- [ ] 跨租户访问被 RLS 阻止（有测试覆盖）
- [ ] 密码 bcrypt 哈希；refresh token rotation 正常
- [ ] 错误响应统一格式：`{"code": "...", "message": "..."}`

### 负责人

- **后端 A**：模型、JWT、RLS、迁移
- **后端 B**：API 路由、测试
- **前端 A**：登录注册页

### 退出条件

进入阶段 3 前：5 个种子用户能注册、登录、创建租户、切换租户。

---

## 四、阶段 3：知识库与 RAG（W5-W7）

### 目标

让用户上传文档、检索内容，这是 GEO 平台的差异化能力。

### 交付物

| # | 交付物 | 说明 |
|---|--------|------|
| 1 | 4 张表 | `knowledge_base`、`document`、`kb_chunk`、`kb_upload_task` |
| 2 | 文档上传 API | 支持 PDF/DOCX/Markdown/TXT，最大 50MB |
| 3 | 异步分块任务 | Celery，chunk_size 500/重叠 100 |
| 4 | Embedding 入库 | OpenAI text-embedding-3-small + embedding 缓存 |
| 5 | RAG 检索 API | Top-K + 相似度阈值 |
| 6 | 知识库前端管理 | 列表、上传、文档列表、检索测试 |
| 7 | 内容预览 | 检索结果高亮引用片段 |

### DoD

- [ ] 上传一份 100 页 PDF，5 分钟内完成分块+embedding
- [ ] 检索相关问题能返回 Top-5 引用片段，p95 < 2s
- [ ] 同一文档重复上传走 embedding 缓存
- [ ] RAG 召回率人工评估 ≥ 70%（20 条标准 query）

### 负责人

- **后端 B**：模型、API、Celery 任务
- **AI 算法 A**：Embedding 选型、分块策略
- **前端 B**：知识库前端

### 退出条件

进入阶段 4 前：内部 3 人用真实资料测试 RAG 召回可接受。

---

## 五、阶段 4：文章生成核心（W8-W11）

### 目标

用户能基于知识库一键生成可发布文章，这是产品核心价值。

### 交付物

| # | 交付物 | 说明 |
|---|--------|------|
| 1 | 5 张表 | `article`、`article_version`、`generation_task`、`prompt_template`、`model_call_log`（分区表） |
| 2 | 5 种预设模板 | 博客 / 产品介绍 / 新闻稿 / SEO 文章 / 社交媒体 |
| 3 | 文章生成 API | 输入：知识库 ID + 模板 + 关键词；输出：文章 + 引用 |
| 4 | 异步生成管线 | outline → section → seo → quality 4 阶段 Celery |
| 5 | 文章编辑器前端 | TipTap / Lexical，富文本 + Markdown 双模 |
| 6 | 简单质量评分 | ContentGuard 简化版（正则 + LLM 自评）合并到 `quality_score` |
| 7 | 历史与版本 | 版本快照 + diff |
| 8 | 流式生成（SSE） | 边生成边返回，参考 LLM 流式输出 |

### DoD

- [ ] 生成 2000 字文章 < 30s（不含 LLM 排队）
- [ ] 5 种模板都能跑通，输出符合模板结构
- [ ] 质量评分与人工评判相关性 ≥ 0.6
- [ ] 生成失败可重试，可降级到备用模型

### 负责人

- **后端 C**：生成管线、Celery、Prompt 模板
- **AI 算法 A**：Prompt 工程、模型路由
- **AI 算法 B**：ContentGuard、流式输出
- **前端 A**：文章编辑器
- **前端 B**：文章列表、历史版本

### 退出条件

进入阶段 5 前：5 个种子客户用 3 种模板各生成 1 篇文章，满意度 ≥ 4/5。

---

## 六、阶段 5：发布与导出（W12-W13）

### 目标

让生成的文章能跑出去，不是停在平台里。

### 交付物

| # | 交付物 | 说明 |
|---|--------|------|
| 1 | 2 张表 | `publish_target`、`publish_log` |
| 2 | 一键导出 | Markdown 复制、HTML 下载 |
| 3 | WordPress REST API 集成 | OAuth + 文章发布 |
| 4 | 发布状态同步 | 成功/失败/草稿 |
| 5 | 简单的发布配置页 | 填写 WP 站点 URL + 凭证 |

### DoD

- [ ] 导出 Markdown/HTML 文件格式正确
- [ ] WordPress 发布成功率 ≥ 95%（10 次测试）
- [ ] 发布失败有明确错误提示和重试入口
- [ ] 发布记录可追溯（成功时间、文章 ID、URL）

### 负责人

- **后端 C**：WP 集成
- **前端 A**：发布配置页

### 退出条件

进入阶段 6 前：完整链路 `上传文档 → 生成文章 → 发布到 WP` 跑通 3 次。

---

## 七、阶段 6：联调 / 压测 / 安全审计（W14-W15）

### 目标

**上线前最后一次体检**。

### 交付物

| # | 交付物 | 说明 |
|---|--------|------|
| 1 | E2E 测试覆盖 | 4 条主链路全部 Playwright 覆盖 |
| 2 | 性能压测报告 | 100 并发、500 并发、1000 并发下的 P95 |
| 3 | 安全审计 | SQL 注入、越权、敏感数据、XSS |
| 4 | 故障演练 | 数据库宕机、Redis 失效、LLM 超时 |
| 5 | 备份恢复演练 | PostgreSQL 每日备份 + 恢复测试 |
| 6 | Runbook | 常见故障处理 SOP |

### DoD

- [ ] 100 并发下 P95 < 2s（非生成 API）
- [ ] E2E 测试 100% 通过
- [ ] 安全审计无 P0 漏洞
- [ ] 备份恢复演练成功

### 负责人

- **全员**：按模块认领
- **苏苏**：组织评审

### 退出条件

进入阶段 7 前：所有 DoD 全绿。

---

## 八、阶段 7：灰度发布与内测（W16）

### 目标

**5-10 个种子客户真实使用**，收集反馈。

### 交付物

| # | 交付物 | 说明 |
|---|--------|------|
| 1 | 5-10 个种子账号 | 人工开通（暂不接计费） |
| 2 | 使用监控看板 | 每日活跃、生成次数、错误率 |
| 3 | 客户反馈表 | 每日收集 |
| 4 | V1.5 范围重排 | 基于真实反馈 |

### DoD

- [ ] 5 个客户连续 7 天有使用记录
- [ ] 关键错误率 < 1%
- [ ] 至少 3 条 P0 反馈被记录
- [ ] V1.5 范围文档定稿

### 负责人

- **苏苏**：客户管理、反馈汇总
- **后端 A**：监控、稳定性

### 退出条件

V1.5 启动会召开、范围确认、ADR-0002 写入。

---

## 九、关键里程碑速查

| 时间 | 里程碑 | 验收方式 |
|------|--------|----------|
| W2 末 | 工程化基础完成 | 7 人能跑通本地，CI 绿 |
| W4 末 | 认证闭环 | 注册登录可用 |
| W7 末 | 知识库可用 | RAG 召回可接受 |
| W11 末 | 文章生成可用 | 5 个客户内测 |
| W13 末 | 发布可用 | WP 集成跑通 |
| W15 末 | 上线就绪 | 性能、安全、压测全过 |
| W16 末 | 正式内测 | 5-10 客户真实使用 |

---

## 十、与 V2 的边界

**V1 不做（明确告知团队不要碰）：**

- ❌ 任何计费/订阅功能（人工收款）
- ❌ GEO 地理位置模块
- ❌ SERP 关键词监控
- ❌ A/B 测试
- ❌ 多平台分发（除 WP）
- ❌ AI 配图
- ❌ 超级管理后台（用 SQLAdmin 凑合）
- ❌ OAuth / SSO
- ❌ 多模态（图片/视频/音频）

**遇到「老板/客户要求加 X 功能」的统一答复：**
> "这个功能 V2 会做，V1.5 排期是 [日期]。如果你能接受推迟到 V1.5，我们现在继续推进核心闭环；如果不能，我们砍掉 [等价工作量] 来换。"

---

## 十一、第一阶段立即开始清单（今天 / 明天）

苏苏你直接照这个推：

- [ ] 把本文件 `v1-development-phases.md` 发到团队群
- [ ] 召开 30 分钟启动会：介绍阶段 1 目标
- [ ] 指定本周质量官（建议后端 A，因为阶段 1 主要是工程）
- [ ] 创建 Git 仓库（GitHub 或 Gitee）
- [ ] 7 人本地 clone 仓库
- [ ] 我会同时把 monorepo 骨架 + 配置文件一并推上来

---

*阶段重排完成。下一步：开始落地阶段 1 的代码。*
